# SnailScriptIIDE
This version of repo has the IDE of snailscript(.ss)

Hello!

Thank you for using our software, if you know what SnailScript is, so no worrys, for the *People who do not know what SnailScript is*, is not a problem. **SnailScript** is a language created by Ocale in the mid 2026. It features **Low level(level 3 -> 4)** to **High Level(Level 1 -> 2)**. Due to some circumstances, the code what we give will run depending on the software you use, so for example you use **VSCode**, then it will recognize has a plain text, samething with *Other text editors/IDE*. Thats why we have came up to create the **Best** version for **SnailScript Code-editor**!.

here are the instruction to get started:

1. Go to "File" and open a folder or file
2. add the extension: *.ss*
3. Start **Coding in SnailScript right in front of you!**

Copyright notes:
The assets we use is managed by Ocale™. Any other assets is not by us. We create everything *from scratch*.

# SnailScript Version
Current version: 1.0
The IDE is mainly created in C#, and python is from *AI*
